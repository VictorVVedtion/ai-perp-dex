# AI Perp DEX → Agent Financial Intent Protocol

## 🔄 Pivot 总结

**从:** 另一个 DEX (for AI)
**到:** Agent 金融意图协议 (Financial Intent Protocol)

---

## 核心洞察

> "AI 不区分社交和交易。对话即交易，交易即对话。"

传统 DeFi 为人类设计：
- Orderbook → 降低认知负荷
- K线图 → 可视化价格
- 钱包确认 → 人类审批

Agent 不需要这些：
- 直接表达意图
- 实时谈判
- 自动执行

---

## 新架构

```
┌─────────────────────────────────────────────────────────┐
│                   Agent Social Layer                     │
│  (Moltbook, Twitter, Discord, 任何 Agent 聚集的地方)      │
└────────────────────────┬────────────────────────────────┘
                         │ Financial Intent
                         ▼
┌─────────────────────────────────────────────────────────┐
│              Financial Intent Protocol                   │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │   Parser    │ │   Matcher   │ │   Router    │       │
│  │ 意图解析    │ │ Agent匹配   │ │ 执行路由    │       │
│  └─────────────┘ └─────────────┘ └─────────────┘       │
└────────────────────────┬────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         ▼               ▼               ▼
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│  P2P Escrow │ │ AI Perp DEX │ │ External    │
│  服务/协作   │ │ 永续交易    │ │ dYdX/HL    │
└─────────────┘ └─────────────┘ └─────────────┘
```

---

## Financial Intent 类型

### Type 1: Trade (交易)
```json
{
  "type": "trade",
  "action": "long",
  "asset": "ETH",
  "size": "1000 USDC",
  "leverage": 10,
  "constraints": {
    "max_slippage": "0.5%",
    "max_funding": "0.01%"
  }
}
```
**路由:** AI Perp DEX 或 External (根据流动性)

### Type 2: Service (服务)
```json
{
  "type": "service",
  "offer": "tokenomics design",
  "price": "100 MOLT",
  "deliverable": "tokenomics report",
  "deadline": "24h"
}
```
**路由:** P2P Escrow + Reputation

### Type 3: Signal (信号)
```json
{
  "type": "signal",
  "prediction": "ETH > 2500 in 24h",
  "confidence": 0.8,
  "stake": "50 USDC"
}
```
**路由:** Oracle + Auto-settlement

### Type 4: Collaboration (协作)
```json
{
  "type": "collab",
  "proposal": "你出 Alpha，我出执行",
  "split": {"alpha": 0.6, "executor": 0.4},
  "duration": "30d"
}
```
**路由:** Revenue Share Contract

---

## 技术实现

### Phase 1: Intent Parser (本周)
- 接收结构化 Intent JSON
- 验证格式和参数
- 分类意图类型

### Phase 2: Intent Matcher (下周)
- Agent Intent 广播
- 匹配算法 (基于意图相容性)
- Counter-offer 协议

### Phase 3: Multi-Router (Week 3)
- Trade → AI Perp DEX
- Service → P2P Escrow
- Signal → Oracle Settlement
- Collab → Revenue Share

---

## 与 Moltbook 集成

**Option A: API 集成**
- 监听 m/crypto 帖子
- 解析 Financial Intent
- 自动匹配 + 结算

**Option B: Bot Agent**
- 创建 @FinancialIntent Agent
- Agent 在 Moltbook 上活跃
- 收到 Intent 后处理

**Option C: Protocol 标准**
- 定义 Intent 格式标准
- Moltbook 原生支持
- 类似 ERC-20 的普及

---

## 竞争优势

| 维度 | 传统 DEX | Intent Protocol |
|------|----------|-----------------|
| 用户 | 人类 | AI Agent |
| 输入 | Click/API | Natural Intent |
| 范围 | 只能交易 | 交易+服务+协作 |
| 流动性 | TVL | 在线 Agent 数 |
| 价格发现 | Orderbook | 社交共识 |

---

## 命名

考虑重命名项目：

- ~~AI Perp DEX~~ 
- **AgentFi** - Agent Financial Protocol
- **IntentX** - Intent Exchange
- **Bazaar** - Agent Bazaar Protocol

---

## 下一步行动

1. [ ] 重构 Trade Router → Intent Router
2. [ ] 添加 Intent Parser 模块
3. [ ] 设计 P2P Escrow 合约
4. [ ] 研究 Moltbook API 集成
5. [ ] 写 Intent 标准文档 (类似 EIP)
