# AI Perp DEX Configuration
# Copy to .env and fill in values

# Database
PERP_DEX_DB=perp_dex.db

# Trading Mode
# sim = 模拟交易 (默认, 安全)
# live = 真实交易 (需要私钥, 有风险!)
TRADING_MODE=sim

# Hyperliquid Configuration (仅 TRADING_MODE=live 时需要)
HL_PRIVATE_KEY=0x...your_private_key...
HL_TESTNET=false

# Builder Fee (收取额外手续费, 可选)
HL_BUILDER_ADDRESS=0xc900999f72D3058604E57746f117a2412d62E44a
HL_BUILDER_FEE_BPS=1

# CORS (逗号分隔的允许域名)
CORS_ORIGINS=http://localhost:3000,http://localhost:8082

# Valid Assets (逗号分隔，留空使用默认)
# VALID_ASSETS=BTC-PERP,ETH-PERP,SOL-PERP

# Rate Limiting
RATE_LIMIT_PER_MINUTE=100

# Logging
LOG_LEVEL=INFO
